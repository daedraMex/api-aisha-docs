info:
  name: Delete SMTP Destination - DELETE /smtp-destinations/{destination_id}/
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base_url}}/api/v1/integration-engine/smtp-destinations/{{destination_id}}/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 204 No Content
    request:
      url: "{{base_url}}/api/v1/integration-engine/smtp-destinations/{{destination_id}}/"
      method: DELETE
    response:
      status: 204
      statusText: No Content
      body:
        type: text
        data: ""
  - name: 404 Not Found - NOT_FOUND
    request:
      url: "{{base_url}}/api/v1/integration-engine/smtp-destinations/does-not-exist/"
      method: DELETE
    response:
      status: 404
      statusText: Not Found
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "error": {
              "code": "SMTP_DESTINATION.NOT_FOUND",
              "message": "SMTP destination not found.",
              "details": null
            }
          }
