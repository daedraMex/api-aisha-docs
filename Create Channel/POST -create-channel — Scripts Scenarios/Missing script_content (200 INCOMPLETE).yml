info:
  name: Missing script_content (200 INCOMPLETE)
  type: http
  seq: 6

http:
  method: POST
  url: "{{base_url}}/api/v1/integration-engine/users/{{user_id}}/create-channel/"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "My Channel No Script Content",
        "description": "Integration test channel",
        "initial_state": "STARTED",
        "metaDataColumn": [
          {
            "name": "PATIENT_ID",
            "type": "STRING",
            "mappingName": "mirth_patient_id"
          }
        ],
        "source": [
          {
            "template_codigo": "channel-reader",
            "properties": {
              "respond_after_processing": true,
              "queue_buffer_size": 1000,
              "response_variable": "None",
              "process_batch": false,
              "first_response": false,
              "processing_threads": 1
            }
          }
        ],
        "destinations": [
          {
            "template_codigo": "file-dest",
            "properties": {
              "host": "ftp.example.com"
            }
          }
        ],
        "prune_metadata_days": 30,
        "prune_content_days": 60,
        "archive_enabled": true,
        "script_type": "DEPLOY"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 OK - INCOMPLETE, xml_generated null
    request:
      url: "{{base_url}}/api/v1/integration-engine/users/{{user_id}}/create-channel/"
      method: POST
      body:
        type: json
        data: |-
          {
            "name": "My Channel No Script Content",
            "description": "Integration test channel",
            "initial_state": "STARTED",
            "metaDataColumn": [
              {
                "name": "PATIENT_ID",
                "type": "STRING",
                "mappingName": "mirth_patient_id"
              }
            ],
            "source": [
              {
                "template_codigo": "channel-reader",
                "properties": {
                  "respond_after_processing": true,
                  "queue_buffer_size": 1000,
                  "response_variable": "None",
                  "process_batch": false,
                  "first_response": false,
                  "processing_threads": 1
                }
              }
            ],
            "destinations": [
              {
                "template_codigo": "file-dest",
                "properties": {
                  "host": "ftp.example.com"
                }
              }
            ],
            "prune_metadata_days": 30,
            "prune_content_days": 60,
            "archive_enabled": true,
            "script_type": "DEPLOY"
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "id": "20202020-2020-2020-2020-202020202020",
            "status": "INCOMPLETE",
            "json_data": { "script_type": "DEPLOY", "script_content": null },
            "xml_generated": null
          }

docs: "A valid script_type is present but script_content key is absent entirely (not present-as-empty-string). Same INCOMPLETE tier as a missing script_type -- explicitly NOT a 400, per spec's resolved note #4."
