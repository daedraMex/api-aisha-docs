info:
  name: Not Found - Unknown source template_codigo
  type: http
  seq: 8

http:
  method: POST
  url: "{{base_url}}/api/v1/integration-engine/users/{{user_id}}/create-channel/"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "HL7-Ingest-Sede-Norte",
        "source": [
          { "template_codigo": "does-not-exist", "properties": {} }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Error - 404 Not Found
    request:
      url: ""
      method: POST
    response:
      status: 404
      statusText: Not Found
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "error": {
              "code": "CHANNEL.TEMPLATE_NOT_FOUND",
              "message": "No template found with codigo='does-not-exist'.",
              "details": null
            }
          }

docs: Passes fail-fast validation (cardinality is fine), fails downstream in the pipeline once the handler tries to look up the template.
