info:
  name: Create Draft - HTTP Listener (None auth)
  type: http
  seq: 1

http:
  method: POST
  url: "{{base_url}}/api/v1/integration-engine/users/{{user_id}}/create-channel/"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "HTTP-Ingest-Sin-Auth",
        "description": "Canal HTTP Listener sin autenticacion, escuchando en un puerto y context path propios",
        "initial_state": "STARTED",
        "source": [
          {
            "template_codigo": "http-listener",
            "properties": {
              "auth_type": "NONE",
              "local_port": 8080,
              "context_path": "/v1/ingest"
            }
          }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Success - 200 OK (INCOMPLETE)
    request:
      url: ""
      method: POST
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "id": "e1f2a3b4-c5d6-4e7f-8a9b-0c1d2e3f4a5c",
            "status": "INCOMPLETE",
            "json_data": {
              "name": "HTTP-Ingest-Sin-Auth",
              "source": [
                {
                  "template_codigo": "http-listener",
                  "properties": {
                    "auth_type": "NONE",
                    "local_port": 8080,
                    "context_path": "/v1/ingest"
                  }
                }
              ]
            },
            "xml_generated": null
          }

docs: "auth_type=NONE: pluginProperties se reconstruye como NoneHttpAuthProperties, sin campos adicionales. No se envian destinations todavia -> INCOMPLETE (200)."
