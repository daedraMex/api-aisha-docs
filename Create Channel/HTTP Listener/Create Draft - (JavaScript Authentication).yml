info:
  name: Create Draft - (JavaScript Authentication)
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/api/v1/integration-engine/users/{{user_id}}/create-channel/"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "HTTP-Ingest-JS-Auth",
        "description": "Canal Javascript Listener con autenticacion por script JavaScript custom",
        "initial_state": "STARTED",
        "source": [
          {
            "template_codigo": "javascript listener",
            "properties": {
              "auth_type": "JAVASCRIPT",
              "script": "if (connectorMessage.getSourceMap().get('Authorization') === 'Bearer valid-token') {\n  return AuthenticationResult.Success();\n}\nreturn AuthenticationResult.Failure();",
              "local_port": 8080
            }
          }
        ]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: Success - 200 OK (INCOMPLETE)
    request:
      url: ""
      method: POST
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "id": "e1f2a3b4-c5d6-4e7f-8a9b-0c1d2e3f4a5c",
            "status": "INCOMPLETE",
            "json_data": {
              "name": "HTTP-Ingest-JS-Auth",
              "source": [
                {
                  "template_codigo": "http-listener",
                  "properties": {
                    "auth_type": "JAVASCRIPT",
                    "script": "if (connectorMessage.getSourceMap().get('Authorization') === 'Bearer valid-token') {\n  return AuthenticationResult.Success();\n}\nreturn AuthenticationResult.Failure();",
                    "local_port": 8080
                  }
                }
              ]
            },
            "xml_generated": null
          }

docs: "auth_type=JAVASCRIPT: script es payload-owned; si se omite, bind_http_listener_source usa el script default de Mirth ('return AuthenticationResult.Success();' con los comentarios de stub)."
