info:
  name: Resume Draft - Complete with draft_id (200 SUCCESS)
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/api/v1/integration-engine/users/{{user_id}}/create-channel/"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "draft_id": "{{draft_id}}",
        "name": "My Channel",
        "description": "Integration test channel",
        "initial_state": "STARTED",
        "metaDataColumn": [
          { "name": "PATIENT_ID", "type": "STRING", "mappingName": "mirth_patient_id" }
        ],
        "source": [
          { "template_codigo": "channel-reader", "properties": { "respond_after_processing": true, "queue_buffer_size": 1000, "response_variable": "None", "process_batch": false, "first_response": false, "processing_threads": 1 } }
        ],
        "destinations": [
          { "template_codigo": "file-dest", "properties": { "host": "ftp.example.com" } }
        ],
        "prune_metadata_days": 30,
        "prune_content_days": 60,
        "archive_enabled": true,
        "script_type": "DEPLOY",
        "script_content": "// PR10c integration test script\nreturn true;"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 OK - SUCCESS (existing draft completed, not 201)
    request:
      url: "{{base_url}}/api/v1/integration-engine/users/{{user_id}}/create-channel/"
      method: POST
      body:
        type: json
        data: |-
          {
            "draft_id": "{{draft_id}}",
            "name": "My Channel",
            "description": "Integration test channel",
            "initial_state": "STARTED",
            "metaDataColumn": [
              { "name": "PATIENT_ID", "type": "STRING", "mappingName": "mirth_patient_id" }
            ],
            "source": [
              { "template_codigo": "channel-reader", "properties": { "respond_after_processing": true, "queue_buffer_size": 1000, "response_variable": "None", "process_batch": false, "first_response": false, "processing_threads": 1 } }
            ],
            "destinations": [
              { "template_codigo": "file-dest", "properties": { "host": "ftp.example.com" } }
            ],
            "prune_metadata_days": 30,
            "prune_content_days": 60,
            "archive_enabled": true,
            "script_type": "DEPLOY",
            "script_content": "// PR10c integration test script\nreturn true;"
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json
      body:
        type: json
        data: |-
          {
            "id": "44444444-4444-4444-4444-444444444444",
            "status": "SUCCESS",
            "json_data": {
              "name": "My Channel",
              "description": "Integration test channel",
              "initial_state": "STARTED",
              "metadata_columns": [ { "name": "PATIENT_ID", "type": "STRING", "mappingName": "mirth_patient_id" } ],
              "source": { "template_codigo": "channel-reader", "properties": { "respond_after_processing": true, "queue_buffer_size": 1000, "response_variable": "None", "process_batch": false, "first_response": false, "processing_threads": 1 } },
              "destinations": [ { "template_codigo": "file-dest", "properties": { "host": "ftp.example.com" } } ],
              "script_type": "DEPLOY",
              "script_content": "// PR10c integration test script\nreturn true;"
            },
            "xml_generated": "<channel version=\"4.4.2\">...<deployScript>// PR10c integration test script\nreturn true;</deployScript>...</channel>"
          }

docs: Same draft_id as the previous INCOMPLETE call, now with destinations filled in. Because the draft already exists (draft_id present), completing it returns 200 -- never 201, which is reserved for brand-new drafts that resolve to SUCCESS on their very first call.
